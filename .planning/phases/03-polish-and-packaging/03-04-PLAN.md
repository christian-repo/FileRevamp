---
phase: 03-polish-and-packaging
plan: 04
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "dotnet pack produces a valid FileRevamp.*.nupkg in src/FileRevamp/nupkg/"
    - "dotnet tool install --global --add-source ./nupkg FileRevamp succeeds from the repo root"
    - "filerevamp --version prints a version string matching 1.0.0"
    - "filerevamp --help runs from any shell prompt after global install"
  artifacts:
    - path: "src/FileRevamp/nupkg/FileRevamp.1.0.0.nupkg"
      provides: "Distributable dotnet tool package"
      contains: ""
  key_links:
    - from: "dotnet tool install (shell)"
      to: "src/FileRevamp/nupkg/FileRevamp.1.0.0.nupkg"
      via: "--add-source ./nupkg"
      pattern: "filerevamp --version"
---

<objective>
Verify that the packaged tool can be installed globally and run from any shell prompt. This is a checkpoint plan: Claude automates the pack and install steps, then pauses for the user to confirm the tool runs correctly from their shell.

Purpose: Satisfy ROADMAP Phase 3 success criterion 1 ("dotnet tool install -g FileRevamp succeeds and filerevamp --version runs from any shell prompt"). This cannot be fully automated because verifying "runs from any shell prompt" requires a human to open a new terminal.
Output: A globally installed filerevamp tool verified by the user.
</objective>

<execution_context>
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/execute-plan.md
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/03-04-SUMMARY.md
</execution_context>

<context>
@D:/REPO/FileRevamp/.planning/ROADMAP.md
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/03-CONTEXT.md
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/03-02-SUMMARY.md
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build, pack, and install FileRevamp as a dotnet global tool</name>
  <files></files>
  <read_first>
    - src/FileRevamp/FileRevamp.csproj — confirm PackageId=FileRevamp, Version=1.0.0, PackageOutputPath=./nupkg are set (from 03-02)
  </read_first>
  <action>
    Run the following sequence of commands and verify each exits 0:

    Step 1 — Build release:
      dotnet build D:/REPO/FileRevamp/src/FileRevamp/FileRevamp.csproj -c Release

    Step 2 — Pack:
      dotnet pack D:/REPO/FileRevamp/src/FileRevamp/FileRevamp.csproj -c Release --no-build

    Confirm that src/FileRevamp/nupkg/FileRevamp.1.0.0.nupkg (or similar version) was created.

    Step 3 — Uninstall any prior global install (idempotent; ignore error if not installed):
      dotnet tool uninstall -g FileRevamp

    Step 4 — Install from local nupkg:
      dotnet tool install -g FileRevamp --add-source D:/REPO/FileRevamp/src/FileRevamp/nupkg

    Step 5 — Verify version in the current shell:
      filerevamp --version

    Report the output of each step inline so the user can see what happened.
  </action>
  <verify>
    <automated>filerevamp --version</automated>
  </verify>
  <done>
    - dotnet build exits 0
    - dotnet pack exits 0 and produces FileRevamp.*.nupkg in src/FileRevamp/nupkg/
    - dotnet tool install exits 0
    - filerevamp --version prints "1.0.0" (or the assembly version string)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Claude ran dotnet pack, installed FileRevamp as a global dotnet tool from the local .nupkg, and verified filerevamp --version in the current shell. The tool is now installed globally.
  </what-built>
  <how-to-verify>
    Open a NEW terminal window (PowerShell or cmd — not the one Claude is using). Then run each of the following commands and confirm the expected output:

    1. filerevamp --version
       Expected: prints "1.0.0" (or a version number — not an error)

    2. filerevamp --help
       Expected: prints usage instructions including --remove, --dry-run, and pattern examples

    3. filerevamp . --remove "_new" --dry-run
       Expected: exits 0 (may say "Renamed: 0" if the current directory has no matching files — that is correct)

    If any command fails with "not found" or "command not recognized", the global tool PATH may not be in your shell's PATH. The dotnet tools directory is typically: %USERPROFILE%\.dotnet\tools (Windows) — add it to PATH if needed.
  </how-to-verify>
  <resume-signal>Type "approved" if all three commands worked correctly, or describe the error you saw.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local nupkg → global tool install | Installing from a local source; not from nuget.org; no supply chain risk |
| Global tool → user shell | filerevamp runs under the user's own permissions; no elevation required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | dotnet tool install from local source | accept | Source is the local repo build output; no remote package fetch in this step |
| T-03-SC | Tampering | no new npm/pip/cargo installs | accept | No package installs beyond the dotnet tool being verified |
</threat_model>

<verification>
After checkpoint approval:
- filerevamp --version prints a version string in a new shell
- filerevamp --help shows usage in a new shell
- Phase 3 ROADMAP success criterion 1 is satisfied
</verification>

<success_criteria>
- dotnet pack exits 0 and produces FileRevamp.1.0.0.nupkg
- dotnet tool install exits 0
- filerevamp --version and filerevamp --help run without error from a new shell prompt
- User has typed "approved" confirming manual verification passed
</success_criteria>

<output>
Create .planning/phases/03-polish-and-packaging/03-04-SUMMARY.md when done
</output>
