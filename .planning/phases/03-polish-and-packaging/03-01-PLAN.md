---
phase: 03-polish-and-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/FileRevamp/Core/MockFileSystem.cs
  - src/FileRevamp/Program.cs
  - src/FileRevamp/Output/FailureLogger.cs
  - src/FileRevamp/Commands/RenameSettings.cs
  - tests/FileRevamp.Tests/Fakes/MockFileSystem.cs
  - tests/FileRevamp.Tests/Core/CollisionResolverTests.cs
  - tests/FileRevamp.Tests/Core/RenameOrchestratorTests.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "MockFileSystem lives only in the test assembly (no test infrastructure in src/)"
    - "All existing tests pass on Windows (no Unix-only absolute paths)"
    - "filerevamp --version displays the assembly version, not a hardcoded string"
    - "FailureLogger writes UTC timestamps with a Z suffix"
    - "Running filerevamp with no --remove or --replace shows a validation error"
  artifacts:
    - path: "tests/FileRevamp.Tests/Fakes/MockFileSystem.cs"
      provides: "In-memory IFileSystem for tests"
      contains: "namespace FileRevamp.Tests.Fakes"
    - path: "src/FileRevamp/Program.cs"
      provides: "Assembly-derived version string"
      contains: "Assembly.GetName().Version"
    - path: "src/FileRevamp/Output/FailureLogger.cs"
      provides: "UTC log timestamps"
      contains: "DateTime.UtcNow"
  key_links:
    - from: "tests/FileRevamp.Tests/Core/CollisionResolverTests.cs"
      to: "tests/FileRevamp.Tests/Fakes/MockFileSystem.cs"
      via: "using FileRevamp.Tests.Fakes"
      pattern: "using FileRevamp\\.Tests\\.Fakes"
    - from: "tests/FileRevamp.Tests/Core/RenameOrchestratorTests.cs"
      to: "tests/FileRevamp.Tests/Fakes/MockFileSystem.cs"
      via: "using FileRevamp.Tests.Fakes"
      pattern: "using FileRevamp\\.Tests\\.Fakes"
---

<objective>
Fix the four code-quality issues identified in the Phase 2 review that must be resolved before Phase 3 test work can proceed: move MockFileSystem out of the production assembly (WR-04), fix cross-platform test paths that break on Windows (IN-02, IN-03), update Program.cs to derive the version from the assembly (IN-01), update FailureLogger to write UTC timestamps (WR-01), and add missing validation when the user provides no --remove or --replace argument (WR-03).

Purpose: Phase 3 integration tests depend on MockFileSystem being in the test assembly; the packaging step requires the version string to come from the assembly; the existing unit tests must pass on Windows before new tests are added.
Output: Cleaned codebase with no test infrastructure in src/, all existing tests passing on Windows, assembly-version wiring, UTC log timestamps, and a no-op validation guard.
</objective>

<execution_context>
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/execute-plan.md
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/03-01-SUMMARY.md
</execution_context>

<context>
@D:/REPO/FileRevamp/.planning/ROADMAP.md
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/03-CONTEXT.md
@D:/REPO/FileRevamp/.planning/phases/02-safety-and-reporting/02-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Move MockFileSystem to test assembly and update consumers</name>
  <files>
    tests/FileRevamp.Tests/Fakes/MockFileSystem.cs,
    src/FileRevamp/Core/MockFileSystem.cs,
    tests/FileRevamp.Tests/Core/CollisionResolverTests.cs,
    tests/FileRevamp.Tests/Core/RenameOrchestratorTests.cs
  </files>
  <read_first>
    - src/FileRevamp/Core/MockFileSystem.cs — full source to copy verbatim (only namespace changes)
    - tests/FileRevamp.Tests/Core/CollisionResolverTests.cs — find all using directives and MockFileSystem references
    - tests/FileRevamp.Tests/Core/RenameOrchestratorTests.cs — find all using directives and MockFileSystem references
  </read_first>
  <action>
    1. Create directory tests/FileRevamp.Tests/Fakes/ (create the file; the directory is implied).
    2. Create tests/FileRevamp.Tests/Fakes/MockFileSystem.cs by copying the full content of src/FileRevamp/Core/MockFileSystem.cs verbatim, changing only the namespace declaration from "namespace FileRevamp.Core;" to "namespace FileRevamp.Tests.Fakes;".
    3. Delete src/FileRevamp/Core/MockFileSystem.cs.
    4. In tests/FileRevamp.Tests/Core/CollisionResolverTests.cs: replace "using FileRevamp.Core;" (if it appears as the only using that covers MockFileSystem) with "using FileRevamp.Tests.Fakes;". Do not remove the "using FileRevamp.Core;" if it is also needed for other types — add "using FileRevamp.Tests.Fakes;" as an additional using instead. The MockFileSystem type reference itself does not need to change.
    5. In tests/FileRevamp.Tests/Core/RenameOrchestratorTests.cs: apply the same using-directive update as step 4.
    6. Run "dotnet build" to confirm no compilation errors before proceeding.
  </action>
  <verify>
    <automated>dotnet build D:/REPO/FileRevamp/FileRevamp.sln</automated>
  </verify>
  <done>
    - tests/FileRevamp.Tests/Fakes/MockFileSystem.cs exists with namespace FileRevamp.Tests.Fakes
    - src/FileRevamp/Core/MockFileSystem.cs is deleted
    - "dotnet build" exits 0 (no CS errors about missing MockFileSystem)
    - CollisionResolverTests.cs and RenameOrchestratorTests.cs each have "using FileRevamp.Tests.Fakes;"
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix cross-platform test paths in CollisionResolverTests and RenameOrchestratorTests</name>
  <files>
    tests/FileRevamp.Tests/Core/CollisionResolverTests.cs,
    tests/FileRevamp.Tests/Core/RenameOrchestratorTests.cs
  </files>
  <read_first>
    - tests/FileRevamp.Tests/Core/CollisionResolverTests.cs — find every occurrence of "/exports" or any other Unix absolute path constant used as a directory argument
    - tests/FileRevamp.Tests/Core/RenameOrchestratorTests.cs — find every occurrence of "/exports" or similar Unix absolute path constants
  </read_first>
  <action>
    Both test files use "/exports" as a hardcoded Unix absolute path. On Windows, Path.GetFullPath("/exports") resolves to the current drive root, causing path-traversal checks in RenameOrchestrator.Plan() to reject all proposals (IN-02, IN-03 from Phase 2 review).

    In both files, replace the "/exports" string constant (or its equivalent field/property) with a platform-neutral expression. The pattern to apply:

    Replace:
      private const string ExportsDir = "/exports";
    With:
      private static readonly string ExportsDir =
          Path.Combine(Path.GetTempPath(), "filerevamp_test_exports");

    (Or if the path is inlined at call sites rather than a constant, replace each inline occurrence with the same Path.Combine expression.)

    Do not change any file-path arguments that are passed to MockFileSystem constructors and are used only for in-memory key lookups — those forward-slash strings are intentionally normalized by MockFileSystem.Normalize(). The fix targets only the directoryPath argument passed to RenameOrchestrator.Plan() and CollisionResolver.Resolve(), which call Path.GetFullPath internally.

    After editing, run "dotnet test" to confirm all pre-existing tests pass.
  </action>
  <verify>
    <automated>dotnet test D:/REPO/FileRevamp/FileRevamp.sln --no-build</automated>
  </verify>
  <done>
    - CollisionResolverTests.cs and RenameOrchestratorTests.cs use Path.Combine(Path.GetTempPath(), ...) for the directory path constant
    - No Unix absolute paths remain as the directoryPath argument passed to Plan() or Resolve()
    - "dotnet test" exits 0 (all pre-existing tests green)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix Program.cs version, FailureLogger UTC timestamp, and missing-operation validation</name>
  <files>
    src/FileRevamp/Program.cs,
    src/FileRevamp/Output/FailureLogger.cs,
    src/FileRevamp/Commands/RenameSettings.cs
  </files>
  <read_first>
    - src/FileRevamp/Program.cs — read the full configure block to see the current SetApplicationVersion call
    - src/FileRevamp/Output/FailureLogger.cs — find the line that formats the log entry with DateTime.Now
    - src/FileRevamp/Commands/RenameSettings.cs — find the Validate() method (or confirm it is absent) to know where to add the guard
  </read_first>
  <action>
    Three independent one-line fixes in three separate files:

    FIX A — Program.cs (IN-01):
    Replace the hardcoded string in config.SetApplicationVersion("1.0.0") with:
      config.SetApplicationVersion(
          typeof(Program).Assembly.GetName().Version?.ToString() ?? "dev");
    Add "using System.Reflection;" at the top of Program.cs if it is not already present. (With top-level statements and implicit usings in net9.0, System.Reflection is typically available without an explicit using, but add it if the compiler reports an error.)

    FIX B — FailureLogger.cs (WR-01):
    Find the line that formats the log entry. It currently uses DateTime.Now. Replace it with DateTime.UtcNow and append "Z" to the format string so the output reads:
      [{DateTime.UtcNow:yyyy-MM-dd HH:mm:ss}Z]
    The rest of the line format is unchanged.

    FIX C — RenameSettings.cs (WR-03):
    Add a Validate() override if one does not exist. The method signature is:
      public override ValidationResult Validate()
    Inside it, return ValidationResult.Error("Specify at least one --remove pattern or --replace operand.") when both RemovePatterns and ReplaceOperations are null or empty. Otherwise return ValidationResult.Success().
    If a Validate() override already exists, add the null/empty check to its body.

    After all three edits, run "dotnet test" to confirm all existing tests still pass (the validation guard must not break any existing test that passes --remove or --replace).
  </action>
  <verify>
    <automated>dotnet test D:/REPO/FileRevamp/FileRevamp.sln</automated>
  </verify>
  <done>
    - Program.cs contains "Assembly.GetName().Version" (grep-verifiable, not hardcoded "1.0.0")
    - FailureLogger.cs contains "DateTime.UtcNow" and the format string ends with "Z]"
    - RenameSettings.cs has a Validate() override that returns ValidationResult.Error when both RemovePatterns and ReplaceOperations are null/empty
    - "dotnet test" exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test assembly → production assembly | Test code references production types; moving MockFileSystem out ensures test infrastructure cannot leak into distributed binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | no new npm/pip/cargo installs in this plan | accept | No package installs; existing NuGet references unchanged |
</threat_model>

<verification>
After all three tasks:
- dotnet build exits 0 with no warnings about missing types
- dotnet test exits 0 (all pre-existing tests pass)
- grep -r "MockFileSystem" src/ returns no results (MockFileSystem removed from production assembly)
- grep "DateTime.UtcNow" src/FileRevamp/Output/FailureLogger.cs returns a match
- grep "Assembly.GetName" src/FileRevamp/Program.cs returns a match
</verification>

<success_criteria>
- MockFileSystem exists only in tests/FileRevamp.Tests/Fakes/ — zero occurrences in src/
- All pre-existing tests pass on Windows (dotnet test exits 0)
- filerevamp --version will display the assembly version (wired via Assembly.GetName().Version)
- Log entries use UTC timestamps with a Z suffix
- Running filerevamp with no --remove or --replace returns a non-zero exit code with the validation error message
</success_criteria>

<output>
Create .planning/phases/03-polish-and-packaging/03-01-SUMMARY.md when done
</output>
