---
phase: 03-polish-and-packaging
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/FileRevamp/Commands/RenameCommand.cs
  - src/FileRevamp/FileRevamp.csproj
autonomous: true
requirements: []

must_haves:
  truths:
    - "RenameCommand accepts an optional IFileSystem constructor parameter — tests can inject MockFileSystem"
    - "In production (no injection), RenameCommand continues to use DryRunFileSystem or FileSystem based on --dry-run flag"
    - "FileRevamp.csproj has PackageId, Version, Authors, Description, PackageLicenseExpression, RepositoryUrl, and PackageReadmeFile set"
    - "dotnet pack produces a .nupkg in src/FileRevamp/nupkg/"
  artifacts:
    - path: "src/FileRevamp/Commands/RenameCommand.cs"
      provides: "IFileSystem injection seam"
      contains: "IFileSystem? fileSystem = null"
    - path: "src/FileRevamp/FileRevamp.csproj"
      provides: "Package metadata for dotnet tool distribution"
      contains: "PackageId"
  key_links:
    - from: "src/FileRevamp/Commands/RenameCommand.cs"
      to: "src/FileRevamp/Core/IFileSystem.cs"
      via: "_injectedFileSystem field"
      pattern: "_injectedFileSystem"
    - from: "src/FileRevamp/FileRevamp.csproj"
      to: "README.md"
      via: "PackageReadmeFile element"
      pattern: "PackageReadmeFile"
---

<objective>
Add an optional IFileSystem constructor parameter to RenameCommand (the same null-coalescing pattern already used for IAnsiConsole) so that integration tests can inject MockFileSystem without touching the disk. Separately, complete the package metadata in FileRevamp.csproj so that dotnet pack produces a distributable .nupkg.

Purpose: The IFileSystem injection is the prerequisite for the in-memory integration tests in plan 03-03. The package metadata is the prerequisite for the install verification in plan 03-04.
Output: RenameCommand with IFileSystem injection seam; FileRevamp.csproj with full package metadata; a passing dotnet build and dotnet pack.
</objective>

<execution_context>
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/execute-plan.md
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/03-02-SUMMARY.md
</execution_context>

<context>
@D:/REPO/FileRevamp/.planning/ROADMAP.md
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/03-CONTEXT.md
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add IFileSystem injection to RenameCommand</name>
  <files>
    src/FileRevamp/Commands/RenameCommand.cs
  </files>
  <read_first>
    - src/FileRevamp/Commands/RenameCommand.cs — read the full file; focus on the constructor (lines ~15-26) and the file system selection block (lines ~58-62) to understand exactly what to change
  </read_first>
  <action>
    RenameCommand currently has a single optional IAnsiConsole constructor parameter. Extend it with a second optional IFileSystem parameter using the same null-coalescing pattern (per D-03 from CONTEXT.md and PATTERNS.md §RenameCommand).

    Constructor change — replace:
      private readonly IAnsiConsole _console;

      public RenameCommand(IAnsiConsole? console = null)
      {
          _console = console ?? AnsiConsole.Console;
      }

    With:
      private readonly IAnsiConsole _console;
      private readonly IFileSystem? _injectedFileSystem;

      public RenameCommand(IAnsiConsole? console = null, IFileSystem? fileSystem = null)
      {
          _console = console ?? AnsiConsole.Console;
          _injectedFileSystem = fileSystem;
      }

    File system selection change — replace the block that reads:
      IFileSystem fileSystem = settings.DryRun
          ? new DryRunFileSystem()
          : new FileSystem();

    With:
      // _injectedFileSystem is non-null only in tests (in-memory MockFileSystem).
      // In production it is always null and the dry-run flag selects the implementation.
      IFileSystem fileSystem = _injectedFileSystem
          ?? (settings.DryRun ? new DryRunFileSystem() : new FileSystem());

    No other changes to the method body. The orchestrator already receives dryRun as a bool argument to Execute(), so injected MockFileSystem works correctly for both dry-run and live-run tests.

    The existing CreateTester() in RenameCommandTests.cs registers:
      new RenameCommand(tester.Console)
    This call is still valid — the second parameter defaults to null and the production path is taken. No change to existing tests is required.
  </action>
  <verify>
    <automated>dotnet build D:/REPO/FileRevamp/FileRevamp.sln</automated>
  </verify>
  <done>
    - RenameCommand constructor signature is: public RenameCommand(IAnsiConsole? console = null, IFileSystem? fileSystem = null)
    - _injectedFileSystem field exists in the class body
    - File system selection uses "_injectedFileSystem ??" pattern
    - dotnet build exits 0
    - dotnet test exits 0 (existing tests still pass — constructor is backward-compatible)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add package metadata to FileRevamp.csproj and verify dotnet pack</name>
  <files>
    src/FileRevamp/FileRevamp.csproj
  </files>
  <read_first>
    - src/FileRevamp/FileRevamp.csproj — read the full file to see the existing PropertyGroup before adding metadata
    - D:/REPO/FileRevamp/README.md — confirm the file exists (required before adding PackageReadmeFile)
  </read_first>
  <action>
    The csproj already has PackAsTool=true, ToolCommandName=filerevamp, and PackageOutputPath=./nupkg. Add the following properties to the same PropertyGroup, after the existing packaging block:

      <PackageId>FileRevamp</PackageId>
      <Version>1.0.0</Version>
      <Authors>FileRevamp Contributors</Authors>
      <Description>Batch file renaming tool with wildcard pattern support for Windows.</Description>
      <PackageLicenseExpression>MIT</PackageLicenseExpression>
      <RepositoryUrl>https://github.com/CodexArtifex19/FileRevamp</RepositoryUrl>
      <PackageReadmeFile>README.md</PackageReadmeFile>

    README.md exists at the repo root (confirmed in discovery). If PackageReadmeFile is set and the file does not exist at pack time, dotnet pack will fail — do not add it if README.md is absent.

    Also add a None item to include README.md in the package (required by NuGet tooling when PackageReadmeFile is set):

      <ItemGroup>
        <None Include="..\..\README.md" Pack="true" PackagePath="\" />
      </ItemGroup>

    After editing, run "dotnet pack src/FileRevamp/FileRevamp.csproj" and confirm a .nupkg file is created in src/FileRevamp/nupkg/.
  </action>
  <verify>
    <automated>dotnet pack D:/REPO/FileRevamp/src/FileRevamp/FileRevamp.csproj -c Release --no-build</automated>
  </verify>
  <done>
    - FileRevamp.csproj contains PackageId, Version, Authors, Description, PackageLicenseExpression, RepositoryUrl, PackageReadmeFile elements
    - A file matching the pattern src/FileRevamp/nupkg/FileRevamp.*.nupkg exists after pack
    - dotnet pack exits 0 with no warnings about missing metadata fields
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → RenameCommand | Injected IFileSystem from tests; production path always uses null default |
| NuGet package → end users | Package metadata sets identity fields; MIT license declared explicitly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | PackageId "FileRevamp" | accept | Single-project tool; package identity matches repo name; no impersonation risk at this scope |
| T-03-SC | Tampering | no new npm/pip/cargo installs | accept | No new package installs; existing NuGet references unchanged |
</threat_model>

<verification>
After both tasks:
- dotnet build exits 0
- dotnet test exits 0 (existing tests still pass with backward-compatible constructor)
- grep "_injectedFileSystem" src/FileRevamp/Commands/RenameCommand.cs returns a match
- grep "PackageId" src/FileRevamp/FileRevamp.csproj returns a match
- src/FileRevamp/nupkg/FileRevamp.*.nupkg exists
</verification>

<success_criteria>
- RenameCommand accepts IFileSystem? as a second optional constructor parameter; null default keeps production behavior unchanged
- All existing tests pass (dotnet test exits 0)
- dotnet pack produces FileRevamp.*.nupkg in src/FileRevamp/nupkg/
- Package has PackageId=FileRevamp, Version=1.0.0, MIT license, and RepositoryUrl set
</success_criteria>

<output>
Create .planning/phases/03-polish-and-packaging/03-02-SUMMARY.md when done
</output>
