---
phase: 03-polish-and-packaging
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - tests/FileRevamp.Tests/Commands/EdgeCaseIntegrationTests.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Wildcard pattern with literal dot and parentheses renames the file correctly (no regex error)"
    - "Two files that compute to the same output name both get renamed in a live run (auto-numbering resolves the collision)"
    - "rename-failures.log pre-present in a directory is never included as a rename candidate even when the active pattern would match its name"
    - "Running against an empty directory exits 0 with Renamed: 0 and no crash"
    - "Unicode filenames (accented characters) are renamed successfully"
    - "Long filenames (~245 chars) are processed without error"
  artifacts:
    - path: "tests/FileRevamp.Tests/Commands/EdgeCaseIntegrationTests.cs"
      provides: "Six integration tests covering all D-02 edge cases"
      contains: "EdgeCaseIntegrationTests"
  key_links:
    - from: "tests/FileRevamp.Tests/Commands/EdgeCaseIntegrationTests.cs"
      to: "src/FileRevamp/Commands/RenameCommand.cs"
      via: "CommandAppTester wiring new RenameCommand(tester.Console)"
      pattern: "new RenameCommand\\(tester\\.Console\\)"
---

<objective>
Write six integration tests in a new EdgeCaseIntegrationTests class, covering all edge cases defined in D-02. Tests run through the full CLI via CommandAppTester (per D-01), matching the pattern established in RenameCommandTests.cs. All tests use temp directories on disk (no MockFileSystem injection needed for these scenarios — the edge cases are about real filesystem behavior with real filenames).

Purpose: Validate that the tool handles all documented edge cases without crashing. These tests are the phase's primary deliverable and directly satisfy ROADMAP Phase 3 success criteria 2 and 3.
Output: tests/FileRevamp.Tests/Commands/EdgeCaseIntegrationTests.cs with six passing tests.
</objective>

<execution_context>
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/execute-plan.md
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/03-03-SUMMARY.md
</execution_context>

<context>
@D:/REPO/FileRevamp/.planning/ROADMAP.md
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/03-CONTEXT.md
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/03-01-SUMMARY.md
@D:/REPO/FileRevamp/.planning/phases/03-polish-and-packaging/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write EdgeCaseIntegrationTests — literal dots/parens, batch collision live run, log-file exclusion variant</name>
  <files>
    tests/FileRevamp.Tests/Commands/EdgeCaseIntegrationTests.cs
  </files>
  <read_first>
    - tests/FileRevamp.Tests/Commands/RenameCommandTests.cs — read the full file; copy the class scaffold, CreateTester(), CreateTempDir(), and Dispose() verbatim; study the assertion patterns before writing the new tests
    - src/FileRevamp/Core/WildcardCompiler.cs — read lines 1-60 to confirm the Regex.Escape-first order that makes literal dots/parens safe
  </read_first>
  <behavior>
    Test 1 — LiteralDotsAndParensInPattern_RenamesCorrectly:
      - File: "report.new.(2024).csv"
      - Args: tester.Run(tempDir, "--remove", ".(2024)")
      - ExitCode == 0
      - Output contains "Renamed: 1" and "Failed: 0"
      - File.Exists(Path.Combine(tempDir, "report.new.csv")) is true
      - File.Exists(Path.Combine(tempDir, "report.new.(2024).csv")) is false

    Test 2 — BatchCollision_LiveRun_BothFilesRenameWithAutoNumbering:
      - Files: "prefix_report.csv", "suffix_report.csv"
      - Args: tester.Run(tempDir, "--replace", "prefix_->", "--replace", "suffix_->")
      - ExitCode == 0
      - Output contains "Renamed: 2" and "Failed: 0"
      - File.Exists(Path.Combine(tempDir, "report.csv")) is true
      - File.Exists(Path.Combine(tempDir, "report(1).csv")) is true
      - Original files no longer exist

    Test 3 — LogFileExcluded_EvenWhenPatternWouldMatchIt:
      - Files: "rename-failures.log", "rename-failures_new.txt"
      - Args: tester.Run(tempDir, "--remove", "rename-") — this pattern would match "rename-failures.log" if not excluded
      - ExitCode == 0
      - Output does NOT contain "rename-failures.log" as a rename source
      - File.Exists(Path.Combine(tempDir, "rename-failures.log")) is true (unchanged)
      - The non-log file "rename-failures_new.txt" IS processed: output contains "rename-failures_new.txt" or "failures_new.txt"
  </behavior>
  <action>
    Create tests/FileRevamp.Tests/Commands/EdgeCaseIntegrationTests.cs.

    File structure:
    - Imports block: copy the six using directives from RenameCommandTests.cs verbatim. Add "using FileRevamp.Core;" only if IFileSystem is referenced; it is not needed for these tests (no MockFileSystem injection here).
    - Namespace: FileRevamp.Tests.Commands
    - Class: public sealed class EdgeCaseIntegrationTests : IDisposable
    - Private field: private readonly List<string> _tempDirs = new();
    - Dispose(): copy verbatim from RenameCommandTests.cs
    - CreateTester(): copy verbatim from RenameCommandTests.cs (registers new RenameCommand(tester.Console) — no IFileSystem injection needed for disk-based tests)
    - CreateTempDir(): copy verbatim from RenameCommandTests.cs

    Write tests exactly as specified in the behavior block above. Use FluentAssertions throughout (.Should().Be(), .Should().Contain(), .Should().BeTrue(), .Should().BeFalse()). Include "because:" parameter on all non-obvious assertions.

    Test naming convention (matches RenameCommandTests.cs style): DescriptiveName_Context_ExpectedOutcome.

    Do NOT use MockFileSystem injection in CreateTester() for this class — all edge-case tests in this plan use real disk I/O via temp directories. The IFileSystem injection from 03-02 is reserved for future in-memory test scenarios.
  </action>
  <verify>
    <automated>dotnet test D:/REPO/FileRevamp/FileRevamp.sln --filter "FullyQualifiedName~EdgeCaseIntegrationTests"</automated>
  </verify>
  <done>
    - EdgeCaseIntegrationTests.cs exists with three test methods: LiteralDotsAndParensInPattern_RenamesCorrectly, BatchCollision_LiveRun_BothFilesRenameWithAutoNumbering, LogFileExcluded_EvenWhenPatternWouldMatchIt
    - All three tests pass (dotnet test filter exits 0)
    - No temp directory leaks (Dispose() cleans up)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add EdgeCaseIntegrationTests — empty directory, unicode filenames, long filenames</name>
  <files>
    tests/FileRevamp.Tests/Commands/EdgeCaseIntegrationTests.cs
  </files>
  <read_first>
    - tests/FileRevamp.Tests/Commands/EdgeCaseIntegrationTests.cs — read the file as created in Task 1 to see the existing class structure before appending new test methods
  </read_first>
  <behavior>
    Test 4 — EmptyDirectory_ExitsZero_WithZeroRenamedCount:
      - Files: none (CreateTempDir() with no arguments)
      - Args: tester.Run(tempDir, "--remove", "_new")
      - ExitCode == 0
      - Output contains "Renamed: 0"
      - No exception is thrown

    Test 5 — UnicodeFilename_RenamedSuccessfully:
      - File: "café_new.csv"  (UTF-8: c-a-f-é with combining accent, then _new.csv)
      - Args: tester.Run(tempDir, "--remove", "_new")
      - ExitCode == 0
      - Output contains "Renamed: 1" and "Failed: 0"
      - File.Exists(Path.Combine(tempDir, "café.csv")) is true
      - File.Exists(Path.Combine(tempDir, "café_new.csv")) is false

    Test 6 — LongFilename_ProcessedWithoutError:
      - File: new string('a', 240) + "_new.csv"  (244 chars total — well within Windows MAX_PATH 255 for just the filename)
      - Args: tester.Run(tempDir, "--remove", "_new")
      - ExitCode == 0
      - Output contains "Renamed: 1" and "Failed: 0"
      - File.Exists(Path.Combine(tempDir, new string('a', 240) + ".csv")) is true
  </behavior>
  <action>
    Add three more test methods to the existing EdgeCaseIntegrationTests class. Do not replace or modify Task 1's test methods — append only.

    For Test 5 (unicode): Use the string literal "café_new.csv" directly in the source file. The file encoding for .cs files is UTF-8 by default in .NET 9 projects; the string will be stored and compared correctly. Assert that the expected output file "café.csv" exists on disk.

    For Test 6 (long filename): Construct the filename at test runtime using new string('a', 240) + "_new.csv". This produces a 244-character filename that fits within Windows MAX_PATH for a standalone filename. The full path will be longer (temp dir prefix + filename), so confirm the combined path stays under 260 characters at test authoring time — Path.GetTempPath() is typically 20-30 chars on Windows, leaving ample headroom.

    For Test 4 (empty): CreateTempDir() with no arguments creates the directory but writes no files. Confirm ExitCode == 0 and "Renamed: 0" is in output.

    Use FluentAssertions throughout. Include "because:" on non-obvious assertions (per D-03: failures in these tests must report "succeed, not fail gracefully").
  </action>
  <verify>
    <automated>dotnet test D:/REPO/FileRevamp/FileRevamp.sln --filter "FullyQualifiedName~EdgeCaseIntegrationTests"</automated>
  </verify>
  <done>
    - EdgeCaseIntegrationTests.cs contains six test methods total (three from Task 1 + three from Task 2)
    - All six tests pass (dotnet test filter exits 0)
    - dotnet test (full suite) exits 0 — no regressions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test input → filesystem | Tests write real files to temp directories; filenames include unicode and long strings that must not cause OS-level errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Denial of Service | long filename test | accept | 244-char filename is well under Windows MAX_PATH; test does not approach system limits |
| T-03-SC | Tampering | no new npm/pip/cargo installs | accept | No new package installs |
</threat_model>

<verification>
After both tasks:
- dotnet test --filter "FullyQualifiedName~EdgeCaseIntegrationTests" exits 0 (six tests pass)
- dotnet test (full suite) exits 0 (no regressions)
- grep -c "\[Fact\]" tests/FileRevamp.Tests/Commands/EdgeCaseIntegrationTests.cs returns 6
</verification>

<success_criteria>
- EdgeCaseIntegrationTests.cs has exactly six [Fact] test methods
- All six tests pass on Windows via dotnet test
- No temp directory leaks (Dispose cleans up after each test class run)
- Test for literal dots/parens asserts file renamed to "report.new.csv" (not an error/exception)
- Test for batch collision asserts both "report.csv" and "report(1).csv" exist on disk
- Test for unicode asserts "café.csv" exists on disk (rename succeeded, per D-03)
- Test for long filename asserts the 240-a.csv file exists on disk (rename succeeded, per D-03)
</success_criteria>

<output>
Create .planning/phases/03-polish-and-packaging/03-03-SUMMARY.md when done
</output>
